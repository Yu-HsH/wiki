# 패킷 14~17 계약 공백 — 단일 기준

갱신 날짜: 2026-09-02 (2차)
기준 커밋: `e1b5546`
이전 기준: `682fc9c`
브랜치: `feat/group-final-gaps`

> ## ⚑ 2026-09-02 (2차) — **결정 3건이 더 확정됐다. 창 범위가 4항목으로 굳었다**
>
> **C3-①** = **컬럼 단위 grant 3개**(§3.2.1에서 해소) · **G2-②** = **회수하지만 순서를 뒤집는다**
> (3코스 창에서 제외 → 14 이후 별도 창 W2″. §3.3-2) · **`highlight_links` 이중 등록** =
> 트랙 C는 `MULTI_ITEM_IDS`만 만진다.
>
> **3코스 창 = 4항목** (17a-1 · C4 CHECK 1건 · C3-① grant 축소 · 15b `total_xp`).
> **§5.5.2의 "함수 삭제 없다" 전제가 정정됐다** — `ensure_today_daily_challenge`는
> `drop function`이 필요하다.
> **파일 소유권·창 절차 초안(T-1~T6)·공유 자원 감사는 `docs/agent/TRACKS.md`가 단일 기준이다.**

> ## ⚑ 2026-09-02 — **판단 8건이 확정됐다**
>
> **G1**(reward 3테이블 = 공통 계약) · **G2**(아이템 이벤트 = `room_events`, 창 불필요) ·
> **G3**(3코스 창 = 15 착수 후 랭킹 도달 시점) · **G11**(통일 대신 5값 유지 + 표시 매핑) ·
> **시안 4건**(G16 = 55 / G17 = 20·15·25 / G18 = 범위 밖 / G19 = `변칙`).
> **세 결정(G1·G2·G11)이 필요한 cutover 창을 하나로 줄였다** — §6.2.
>
> **⚠ 이 갱신에서 이전 조사의 오류 하나를 정정했다** — `time_limit`·`grace_timeout`·
> `disconnected_timeout`에 **"쓰는 경로가 없다"는 서술은 틀렸다.** 세 값은 전부
> finalizer가 기록하고 있다. **§3.4.1**

> **이 문서는 미구현 패킷 4개(14 1:1 아이템 / 15 XP·레벨·랭킹 / 16 업적·보상 /
> 17 탐험·프로필·게스트)의 계약을 고정하기 위한 작업 기준이다.**
> 계약 고정이 여러 세션에 걸치므로 조사 결과를 여기 모은다.
>
> **현재 상태의 단일 기준은 `docs/agent/CURRENT.md`다.** 이 문서는 그 §5.0 C 항목의 상세이며,
> 판정·수치가 어긋나면 `CURRENT.md`가 우선한다.

**근거가 된 조사 3회 (전부 2026-09-02, 코드·문서만. 운영 DB 미접근):**

| # | 범위 | 산출 |
|---|---|---|
| 1 | 패킷 간 의존 관계 · 계약 공백 | §1·§2·§3 |
| 2 | 모바일 시안 인라인 대조 · 3코스 전환 | §4·§5 |
| 3 | **78화면 × 10섹션 전수 대조** | §4 |

---

## 1. 패킷별 현황

| | 패킷 | 문서 | 기존 구현 | 새 테이블 | 규모 `[추정]` |
|---|---|---:|---|---:|---|
| **14** | 1:1 아이템 | 134줄 | **있음 — 클라이언트 전용.** `data/items.js`·`utils/itemSystem.js`·`components/ItemBar.jsx` (316줄), inventory가 **localStorage** (`MultiplayerGamePage.jsx:419`) | 2~3 | **大** |
| **15** | XP·레벨·랭킹 | 126줄 | **없음.** `profiles`에 레벨 컬럼 없고 `user_profile_stats`는 카운트 6개뿐 | 1~2 | **小~中** |
| **16** | 업적·보상 | 247줄 | **없음** | **7** | **最大** |
| **17** | 탐험·프로필·게스트 | 140줄 | **부분.** `daily_challenges`가 **하루 1코스** | 2~4 | **中** |

### 1.1 14가 이미 갖고 있는 서버 자산 `[코드]`

**서버 권위 V2가 아이템 자리를 미리 파 놓았다.** 14의 본체는 신규 구현이 아니라 **서버 권위 이전**이다.

| 자산 | 위치 | 내용 |
|---|---|---|
| `event_type` CHECK | `20260814090000:55-58` | **`FORCED_LINK`·`UNDO`·`RANDOM_TELEPORT`·`SWAP`·`REWIND`** — 잘못된 링크·되돌리기·특수:임의·맞교환·역사 되감기가 이미 계약에 있다 |
| `item_event_id`·`undone_event_id` | `:70-71` | 아이템 이벤트 연결 컬럼. 현재 미사용 |
| `game_mutation_requests` | `:87-96` | 멱등성 원장 (`operation` + `response`) |
| `apply_duel_swap_v2` | `20260814094000` | **의도적 비활성 스텁.** `SWAP_DISABLED`만 반환하며, 주석이 선행 조건을 열거한다 — **"server inventory, consumption ledger, ownership checks, and cooldown expiry"** |

### 1.2 아이템 ID 매핑 — 11 → 11이 아니다 `[코드]`

| 현재 | 목표 | 판정 |
|---|---|---|
| `blind` / `random_link_move` / `search_once` / `cleanse_shield` / `go_back` / `random_teleport` / `swap_current` | `ink_blind` / `wrong_link` / `document_find` / `edit_protection` / `rollback` / `special_random` / `document_swap` | **대응 7종.** `cleanse_shield`↔`edit_protection` 수치 일치는 **확인 필요** |
| `highlight_links` · `translate_current` · `double_blind` | — | **제거** (`01-CONFIRMED-SPEC.md` §5.6) |
| `mini_game` | — | **기본 지급 제외. 삭제 금지** |
| — | `link_censorship` · `link_preview` · `backlink_reflect` · `history_rewind` | **신규 4종** |

`swap_current`는 클라이언트(`DISABLED_DUEL_ITEM_IDS`)와 서버(`SWAP_DISABLED`) **양쪽에서 이미 비활성**이다.

---

## 2. 의존 관계

### 2.1 확정된 의존

| 방향 | 근거 |
|---|---|
| **15 → 16** | 16 §6 이벤트 계약이 **`XP granted/level changed`를 소비 이벤트로 명시**. 그리고 16의 "업적 XP는 주간 점수 제외"는 15 ledger의 `source type`이 있어야 성립한다 |
| **14 → 16** | 아이템 근거가 필요한 업적 **5개** — 일반 `완벽한 대응`(2.4) + 히든 `hidden_attack_helped`·`hidden_return_to_sender`·`hidden_swap_win`·`hidden_random_win` |
| **17 ↔ 16** | **양방향.** 16 §5.3이 `reward_catalog`·`user_reward_inventory`·`user_profile_equipment`를 **정의**하고 17 §5가 같은 3분리를 **요구**한다. 게다가 16의 `onboarding_profile_complete` 조건이 17의 장착 이벤트를 소비한다 (§6 "profile cosmetic equipped") |
| **17 → 15** | **문서 어디에도 없던 의존.** 15 §5.1(코스별 독립 랭킹)·§3(코스별 최초 완주 25 XP)은 `course_slot`이 없으면 정의 자체가 성립하지 않는다 → **§5** |
| 14 ↮ 15 | **없음.** 1:1 XP는 승/패/기권만 보고 아이템 여부를 보지 않는다 (15 §1) |

**17↔16의 양방향은 순환이 아니다.** 테이블 소유권을 한쪽에 못 박으면 풀린다 → **G1**.

### 2.2 공유 테이블

| 테이블 | 14 | 15 | 16 | 17 | 충돌 |
|---|:-:|:-:|:-:|:-:|---|
| `reward_catalog`·`user_reward_inventory`·`user_profile_equipment` | | | **정의** | **요구** | **충돌 — G1** |
| XP ledger | | **정의** | 기록 | | 소유 = 15 |
| `daily_challenges` | | 읽기 | 읽기 | **재설계** | 소유 = 17 |
| `profiles` / `user_profile_stats` | | **레벨 추가** | | **전적 확장** | **확인 필요 — G6** |
| `game_move_events` | **확장** | | 읽기 | 읽기 | 없음 |

### 2.3 공유 화면

| 화면 | 14 | 15 | 16 | 17 | 비고 |
|---|:-:|:-:|:-:|:-:|---|
| **결과** | ✓ | ✓ | ✓ | ✓ | **4개 전부.** Freeze v1 `ResultShell`이 순서를 분리한다 — 결과▸기록▸XP▸업적/보상▸경로▸행동 |
| **프로필** | | ✓ | ✓ | ✓ | 레벨(15) / 업적·inventory(16) / 카드·전적·장착(17) |
| **랭킹** | | ✓ | | ✓ | 주간·레벨(15) / 오늘 3코스(15+17) |
| 1:1 게임 | ✓ | | | | 단독 |
| 업적·보상함 | | | ✓ | | 단독 |

### 2.4 병렬 / 순차

| 조합 | 판정 |
|---|---|
| **14 ∥ 15** | **병렬 가능** — 테이블 0 중복, 결과 화면의 다른 영역 |
| **14 ∥ 17** | **병렬 가능** — 접점 없음 |
| **15 ∥ 17a** | **병렬 가능** — 단 코스 랭킹은 17a-1 이후 |
| 15 ∥ 17b | **주의** — 둘 다 `profiles`. 컬럼 계약 선합의 필요 |
| **16 ∥ 무엇이든** | **불가** — 14·15·17 산출물을 전부 소비 |
| 16 ∥ 17b | **금지** — 같은 테이블을 정의한다 |

---

## 3. 계약 공백 — 우선순위 정렬

**판정 기준:** `정의` = 그대로 구현 가능 / `부분` = 이름·항목만 있고 타입·제약 없음 / `공백` = 문서에 없음.

| 축 | 14 | 15 | 16 | 17 |
|---|---|---|---|---|
| 테이블 스키마 | **공백** | **부분** (ledger 필드 4개 이름만) | **부분** (7테이블 + 필드 목록, 타입 없음) | **공백** |
| RPC 시그니처 | **공백** (단 `apply_duel_swap_v2`가 선례) | **공백** | **공백** | **공백** |
| 화면 호출 지점 | **정의** | **정의** | **정의** | **정의** |
| RLS 방향 | **공백** | **공백** | **공백이며 가장 어렵다** (hidden 비노출) | **공백** |
| Realtime | **공백 — G2** | 불필요로 읽힘 | 불필요 | 불필요 |
| 도메인 규칙 | **완전 정의** | **완전 정의** | **정의** (업적 31 + 이벤트 9) | **정의** |

### 3.1 해소된 공백 (2026-09-02) `[사용자 결정]`

| # | 결정 | 상세 |
|---|---|---|
| **G1** | **reward 3테이블은 공통 계약이다.** `reward_catalog`·`user_reward_inventory`·`user_profile_equipment`를 `10-CODE-MASTER-TODO.md` §4로 빼고 **16·17 모두 소비자로 둔다. 어느 패킷에도 소유권을 주지 않는다** — 한쪽에 주면 나머지가 대기한다 | §6.2 W0 |
| **G2** | **아이템 이벤트는 `room_events`로 나른다.** publication에 이미 포함돼 창이 불필요하다. `game_move_events`는 publication 추가 = 새 창이 선행 조건이므로 쓰지 않는다. **이벤트 성격상으로도 `room_events`가 맞다** | **§3.3** |
| **G3** | **3코스 창은 15 착수 후, 코스별 랭킹 도달 시점에 연다.** 15의 ledger·레벨·주간 랭킹은 3코스와 무관하므로 먼저 진행한다 | **§5.5** |
| **G11** | **통일이 아니라 5값을 살린다.** 시안 07 RESULT가 4개 용어를 표시하고 `result_status` 2값으로는 구분되지 않는다 | **§3.4 — 전제 하나가 정정됐다** |
| **G12** | Daily 3코스 순차 해제 = **시안 오류.** unlock 상태를 만들지 않는다 | §4.1 |
| **G13** | `Lv.27`·`340/400`·`1,240 XP` = **목업 샘플.** §7.3 공식 불변 | §4.1 |
| **G15** | 칭호 `길을 잇는 탐험가` = **예시 문구.** 신규 칭호 없음 | §4.1 |
| **G16** | 그룹 2위 XP = **55.** 모바일의 40이 오류 | §4.1 |
| **G17** | 싱글 완주 XP = **20 / 15 / 25.** Freeze v1 `05-05`의 `+40`이 오류이고 `03-02`가 맞다 | §4.1 |
| **G18** | `오늘의 발견` 카드 = **범위 밖.** 패킷 17 계약에 넣지 않는다 | **§7** |
| **G19** | 5번째 슬롯 = **`변칙`.** 모바일의 `가변`·`인장`이 오류 | §4.1 |

### 3.2 W0 계약 고정 — **G1·G4·G6·G10·G11이 문서로 동결됐다 (2026-09-02)**

**`docs/contracts/`가 신설됐다.** `10-CODE-MASTER-TODO.md` §4가 가리키던 자리를 실제 계약으로
구체화한 것이며, **소유자는 전부 `공통`이라 어느 패킷도 재정의하지 않는다.**

| 계약 | 닫는 공백 | 산출 | DDL |
|---|---|---|---|
| [C1 보상 3테이블](../contracts/C1-REWARD-TABLES.md) | **G1** | `reward_catalog`·`user_reward_inventory`·`user_profile_equipment` DDL + RPC 3 | 신규 테이블 3 |
| [C2 XP 원장](../contracts/C2-XP-LEDGER.md) | **G4** | `xp_ledger` DDL + **`xp_class` 3종** + `source_type` 14종 + RPC 3 | 신규 테이블 1 |
| [C3 레벨 저장 위치](../contracts/C3-LEVEL-STORAGE.md) | **G6** | **`profiles.total_xp`** + `level_from_total_xp()`. **레벨은 저장하지 않는다** | `profiles` 컬럼 1 |
| [C4 결과 사유 어휘](../contracts/C4-RESULT-REASON.md) | **G11** | 저장 어휘 7축 전수 + **4용어 매핑** + CHECK 2건 판정 | **3코스 창에 묶는다** |
| [C5 프로필 카드](../contracts/C5-PROFILE-CARD.md) | **G10** | 렌더 계약 + 공통 컴포넌트 2개 | **없음** |

### 3.2.1 미해소 공백 — 우선순위 순

| # | 공백 | 패킷 | 우선순위 |
|---|---|---|---|
| **G7** | 아이템 ID 최종 확정 (기존 7개 재사용 vs 신규 발급). **event_type 명명이 여기 딸린다** | 14 | **1 — 14 착수 전** |
| ~~**C3-①**~~ | ~~`profiles`의 클라이언트 update 경로를 좁힐 것인가~~ → **해소 (2026-09-02)** `[사용자 확정]`. **컬럼 단위 grant 3개**(`nickname` · `profile_image_url` · **`updated_at`**). `updated_at`이 빠지면 배포된 프론트의 두 update가 깨진다는 실측이 계약 문구를 정정했다 — `C3-LEVEL-STORAGE.md` §0·§5.1 | 15 | **해소** |
| **G5** | hidden 업적 비노출 메커니즘 미정 (RLS·뷰·RPC 중 무엇인가) | 16 | 3 — 설계 난제 |
| **C2-③** | 한 완주가 랜덤·오늘 코스에 동시 해당할 때 XP를 겹쳐 주는가. **멱등 유니크의 열 구성이 갈린다** | 15 | 4 |
| **C5-②** | 참가자 행에 칭호를 보이려면 `room_players` 스냅샷 확장이 필요하다 → **DDL 여부가 갈린다** | 17 | 5 |
| **G9** | 결과 화면의 업적 동시 달성 반환 형식 | 16 | 6 |
| **G14** | `FALLBACK_DAILY_POOL`이 "모든 사용자 동일 코스"를 위반 (클라이언트 하드코딩 7개) | 17a | 7 — 3코스 작업에 묶는다 |
| **G8** | `onboarding_full_avatar` 운영 사용 여부 **미확인** | 16 | 8 — 운영 조회 1회. `target-level` 확인과 묶을 수 있다 |
| **C1-④** | **시스템 기본 프로필 아이콘 4~6종의 ID와 지급 방식** — 스펙이 개수도 ID도 정하지 않았다 | 17 | 9 |
| **G20** | 업적명 `다섯 걸음 승부사`(16에 없음) · `앞서가는 탐험가`(16 §3 "추후 활성화") | 16 | 낮음 |

> **계약이 새 공백 4건을 드러냈다** (C3-① · C2-③ · C5-② · C1-④).
> **DDL을 실제로 써 보기 전에는 보이지 않던 것들이다** — 특히 **C3-①은 보안 문제**다.

### 3.3 G2 상세 — `room_events`가 이미 그 경로다 `[코드]`

**결정된 경로가 이미 쓰이고 있다.** 1:1 아이템 이벤트는 **오늘도 `room_events`로 흐른다** —
`pages/MultiplayerGamePage.jsx:190-204`의 `emitRoomEvent`가 **클라이언트에서 직접 INSERT**하고,
`:1023-1037`의 realtime 구독이 그것을 받는다. 현재 event_type: `blind` · `double_blind` ·
`random_link_move` · `translate_current` · `mini_game_choice` · `mini_game_start` · `mini_game_reward`.

| 축 | 상태 | 14의 계약에 미치는 영향 |
|---|---|---|
| **publication** | `room_events`는 4테이블에 **포함돼 있다** | **창이 불필요하다.** 이것이 이 결정의 이유다 |
| **`event_type` 제약** | `text not null`, **CHECK 없음** (`baseline:643`) | **새 event_type 추가에 DDL이 필요 없다.** 값 집합은 코드 계약으로만 존재한다 → **그래서 카탈로그 문서화가 더 중요하다** |
| **payload** | `jsonb not null default '{}'` (`baseline:644`) | **형식이 자유롭다.** 아이템 이벤트 payload 스키마를 14가 직접 정해야 한다 — 최소 `item_id` · `slot_role` · `target_user_id` · `result`(사용/차단/반사/무효) · `server_timestamp` · **`game_move_events.id` 연결키** |
| **RLS — 정책 2개** | `Players can read events in their rooms`(SELECT, baseline) + `Duel players can insert their own room events`(INSERT, phase2c `:900`) | **SELECT는 그대로 쓴다.** 문제는 INSERT다 — 아래 |
| **권한** | `GRANT ALL`이 `anon`·`authenticated`·`service_role`에 있다 (`baseline:1473-1475`). **`20260814093000` cutover는 `game_rooms`·`room_players`·`game_records`·`match_history`만 회수했고 `room_events`는 건드리지 않았다** | **클라이언트가 임의 이벤트를 넣을 수 있다** |

> **이 결정의 핵심 부채는 전송 경로가 아니라 쓰기 주체다.**
> 패킷 14는 "모든 사용·차단·반사·소비를 서버가 확정한다"(spec §5.1)를 요구하는데,
> 지금은 **클라이언트가 직접 INSERT**한다. 코드가 이미 그 위험을 알고 있다 —
> `MultiplayerGamePage.jsx:928-930`이 **"A forged room_events row must not move or mutate this client"**
> 주석과 함께 비활성 SWAP 이벤트를 무시한다.
>
> **따라야 할 선례가 같은 테이블 안에 있다.** 관전 이모티콘은 event_type `group_spectator_emoji`로
> **`send_group_spectator_emoji_v13`(SECURITY DEFINER)가 서버에서 INSERT**한다
> (`20260814123000:136-145`). **14도 같은 형태로 간다 — 아이템 사용 RPC가 이벤트를 쓰고,
> 클라이언트는 읽기만 한다.**

**기존 이모티콘 이벤트와의 구분은 이미 동작한다** `[코드]`:
`GroupGamePage.jsx:756`이 `payload.new.event_type !== "group_spectator_emoji"`면 즉시 반환하고,
`MultiplayerGamePage.jsx:1032`는 `event.user_id === user?.id`면 반환한 뒤 `handleIncomingEvent`가
event_type으로 switch한다. **두 소비자가 서로의 이벤트를 무시하는 구조가 이미 있으므로
새 아이템 event_type을 추가해도 그룹 경로에 영향이 없다.**

**14가 결정해야 할 것 3가지:**

1. **event_type 명명 규칙.** 현재 값들은 아이템 ID를 그대로 쓴다(`blind`). 새 카탈로그로 가면
   ID가 바뀌므로(**G7**) event_type도 함께 바뀐다. `duel_item_used` 하나로 묶고 payload에
   `item_id`를 넣는 편이 CHECK 없는 컬럼에서 값 폭발을 막는다 — **확인 필요**.
2. ~~**클라이언트 INSERT 권한을 회수할 것인가.**~~ → **회수한다. 단 3코스 창이 아니다**
   `[사용자 확정, 2026-09-02]`. 회수하면 위조 경로가 닫히지만 **`room_events`에 대한 DDL이므로
   창이 필요하다.** ~~회수는 3코스 창(§5)에 묶는 것이 자연스럽다~~ — **그 판단이 뒤집혔다.**

   **순서를 뒤집은 이유** `[코드]`: 배포된 프론트가 **10곳에서 직접 INSERT한다**
   (`MultiplayerGamePage.jsx:191-204`의 `emitRoomEvent`, 호출 `:722 :747 :751 :755 :784 :802
   :808 :818 :849 :873`). 3코스 창에서 회수하면 **1:1 아이템이 즉시 깨지고**, 프론트를 배포해도
   **이미 로드된 구버전 번들**이 계속 INSERT한다 — **8월 창에서 게이트가 담당했던 drain 문제가
   되돌아온다** (§5.5.2, U3). **게이트는 2026-09-02에 해제됐다** (`AGENTS.md` §1.1).

   **확정된 순서: ① 14가 서버 INSERT(SECURITY DEFINER)로 전환 → ② 프론트 배포 →
   ③ `grep 'from("room_events").insert'` 0건 확인 → ④ 별도 창에서 회수.**
   선례는 `send_group_spectator_emoji_v13` (`20260814123000:136-145`) `[코드]`.
   그때까지 방어는 지금처럼 **수신 측 무시**에 의존한다 (`MultiplayerGamePage.jsx:928-930`).
   상세: `docs/agent/TRACKS.md` §7.4.
3. **`mini_game_*` 3종의 처리.** 확정 스펙 §5.6이 미니게임을 기본 지급에서 뺐지만
   `AGENTS.md` §4가 임의 삭제를 금지한다. **event_type을 남길지 정해야 한다.**

### 3.4 G11 상세 — reason 어휘가 넷이다 `[코드]`

| 축 | 값 집합 | 출처 |
|---|---|---|
| `single_game_runs.status` | `active` · `completed` · `abandoned` · `expired` | `20260814091000:19` |
| `group_match_results.result_status` | `finished` · `retired` | `20260807003609:89` |
| `group_match_results.retire_reason` | `time_limit` · `grace_timeout` · `forfeited` · `left` · `disconnected_timeout` (**5개**) | `20260807003609:91-92` |
| `leave_group_player`가 **받는** reason | **`left` · `forfeited` (2개뿐)** | `20260814103000:920` |
| `game_records.result_status` | default `'completed'` — **CHECK 없음** | `20260814090000:129` |
| `game_rooms.match_end_reason` | **CHECK 없음** | `20260814090000:105` |
| `10-CODE-MASTER-TODO.md` §4 목표 | `FINISHED` · `FORFEIT` · `RETIRE` · `CANCELLED` | 문서 |

**어느 것도 §4의 4값과 일치하지 않고, 두 컬럼은 제약이 없다.**

**결정 (2026-09-02): 통일하지 않는다. 5값 어휘를 살려 표시 계층에서 매핑한다.**
시안 §07 RESULT가 **완주 / 리타이어 / 몰수 / 기권** 4개 용어를 쓰고 `몰수 — 재접속 유예 종료`까지
표시하는데, `result_status` 2값으로는 구분되지 않기 때문이다.

#### 3.4.1 ⚠ 정정 — 세 값에는 이미 기록 경로가 있다

> **이전 조사(2026-09-02 오전)가 "`time_limit`·`grace_timeout`·`disconnected_timeout`은
> 쓰는 경로가 없다"고 적었다. 그것은 틀렸다.** `leave_group_player`만 보고 판단한 결과다.
> **세 값은 전부 finalizer가 기록하고 있다** `[코드]`:

| 값 | 기록 지점 |
|---|---|
| `time_limit` | `finalize_group_room_if_expired` — `20260807003609:440`, v13 `20260814103000:502` |
| `grace_timeout` | 같은 함수 — `20260807003609:438`, v13 `:501` |
| `disconnected_timeout` | 같은 함수의 분기 — `20260807003609:451`, v13 `:513` (`when player_status = 'disconnected'`). **추가로 `20260814091000:957·962`** (재접속 마감 만료) |
| `left` · `forfeited` | `leave_group_player`의 `p_retire_reason` — `20260814103000:971` |

**즉 5값 어휘는 이미 전부 살아 있다.** `retire_reason`은 `room_players`에 기록되고
`group_match_results`로 전파된다 (`20260814103000:524-539`).

**따라서 G11의 실질 내용이 바뀐다 — "경로를 만드는 일"이 아니라 아래 셋이다.**

#### 3.4.2 실제로 해야 할 것

| # | 항목 | 내용 |
|---|---|---|
| **①** | **표시 매핑 정의** | DB 5값 → 시안 4용어. **`leave_group_player`를 확장할 필요가 없다** — 사용자 명시 이탈은 `left`/`forfeited` 둘로 충분하고 나머지 셋은 서버가 자동으로 붙인다. **매핑 초안:** `finished`→완주 / `forfeited`→기권 / `left`→기권(이탈) / `time_limit`·`grace_timeout`→리타이어 / `disconnected_timeout`→**몰수**. 시안의 `몰수 — 재접속 유예 종료`가 마지막과 정확히 대응한다 |
| **②** | **CHECK 없는 두 컬럼** | `game_records.result_status`(default `'completed'`)와 `game_rooms.match_end_reason`은 **자유 텍스트**다. **CHECK 추가는 DDL** → 창 대상. **다만 지금 잘못된 값이 들어가고 있다는 증거는 없다** — 쓰는 곳이 서버 함수뿐이다. **우선순위는 낮고 3코스 창에 묶을 수 있다** |
| **③** | **싱글·1:1 어휘와의 정렬** | `single_game_runs.status`(4값)와 그룹 어휘가 다르다. **통일하지 않기로 했으므로 이것도 매핑 대상**이며, 시안 §07의 1:1 표기(`WIN_NORMAL`·`승리 상대 기권/이탈`)가 그 축의 요구다 |

**운영 DDL이 필요한가:** **①은 불필요**(전부 읽기·표시), **③도 불필요**, **②만 필요**하다.
**②는 단독으로 창을 열 만한 항목이 아니므로 3코스 창(§5)에 묶는다.**

---

## 4. 시안 대조 (78화면 × 10섹션)

**두 시안은 `docs/design/`에 있고 bundled 형식이라 원문 검색이 되지 않는다.**
매니페스트를 디코드해 대조했다 `[산출물, 2026-09-02]`.

| 파일 | 크기 | 구조 |
|---|---:|---|
| `Wiki Race Wireframe Freeze v1 FINAL.html` | 947,543 B | 섹션 00~08, **화면 77개 + 00 INDEX** |
| `Wiki Race Mobile Visual Validation v0.1 (Standalone).html` | 4,334,231 B | 섹션 01~10 + `SPEC CHECK`·`FLAGS` 블록 |

### 4.1 시안 판정 — **전부 해소 (2026-09-02)** `[사용자 판정]`

| # | 항목 | 판정 | 구현 영향 |
|---|---|---|---|
| **G12** | Daily 3코스 순차 해제 | **시안 오류.** 확정 §3.3 "모든 사용자에게 같은 세 개의 코스를 제공"이 맞다. **Freeze v1 `07-22 TodayStates`도 잠금 없이 `0/3·1/3·3/3` 완료 수만 센다** | **unlock state 테이블·컬럼을 추가하지 않는다** |
| **G13** | `Lv.27` · `340/400 XP` · `1,240 XP` | **목업 샘플.** §7.3 공식 불변. **Freeze v1이 같은 `340/400`을 `Lv.12`에도 쓴다** — 계산값일 수 없다 | **서버 공식 계산값만 사용한다** |
| **G15** | 칭호 `길을 잇는 탐험가` | **예시 문구** | **16의 확정 칭호 목록만 사용, 신규 추가 없음** |
| **G16** | 그룹 2위 XP | **모바일 오류. 55가 맞다** — 확정 §7.1·15 §1·Freeze v1이 전부 55 | **그룹 2위 = 55 XP** |
| **G19** | 5번째 슬롯 명칭 | **모바일 오류. `변칙`이 맞다** — 확정 §5.1·14 §3·Freeze v1이 전부 `변칙` | **`인장`을 아이템 ID로 쓰지 않는다** |
| **G17** | Freeze v1 내부 불일치 | **`05-05`의 `+40`이 오류, `03-02`의 `20/15/25`가 맞다.** 확정 §7.1과 15 §1이 20/15/25로 일치하고 **40은 어느 값도 아니며 조합으로도 나오지 않는다.** 같은 행 부제 `개인 최고 경신`에 해당하는 보너스 XP가 확정 스펙에 없다 | **싱글 완주 XP = 20 / 15 / 25** |
| **G18** | `오늘의 발견` 카드 | **범위 밖.** Freeze v1에 없고 모바일에만 있는 신규 IA | **패킷 17 계약에 포함하지 않는다** → **§7** |

전문·근거는 **`docs/design/MOBILE-VALIDATION-CORRECTIONS.md`**.

> **Freeze v1도 무오류가 아니다 (G17).** 같은 파일의 `03-02`와 `05-05`가 어긋난다.
> **Freeze v1을 SSOT로 인용할 때 화면 단위로 확정 스펙과 대조해야 한다** —
> 파일 전체가 자동으로 정합하지 않는다.

### 4.2 두 시안이 같은 화면에서 다른 데이터를 요구하는 곳 — **4건 전부 해소**

| 화면 | Freeze v1 | 모바일 | 확정값 | 판정 |
|---|---|---|---|---|
| **Today 3코스** (`02-01`·`07-22` ↔ `§02`) | 잠금 없음, 완료 수만 | `다음 코스` / `잠김 해제됨` | 잠금 없음 | **Freeze v1이 맞다 — G12** |
| **그룹 결과 2위 XP** (`05-03`·`06-10` ↔ `§07`) | **+55 XP** | +40 XP | **55** | **Freeze v1이 맞다 — G16** |
| **5번째 슬롯** (`04-03` ↔ `§04`) | **`변칙 · 공격`** | `가변`·`인장` | **변칙** | **Freeze v1이 맞다 — G19** |
| **HOME 구성** (`02-01` ↔ `§02`) | `오늘의 발견` 없음 | `오늘의 발견` 있음 | 없음 | **범위 밖 — G18** |

> **네 건 모두 Freeze v1 쪽이 확정값과 일치했다.** 모바일 보드가 변환 과정에서 어긋난 것이다.
> **다음에 시안이 갱신되면 같은 방향의 대조를 먼저 한다.**

### 4.3 시안이 요구하는데 패킷 문서에 없는 데이터

| 요구 | 화면 | 패킷 | 상태 |
|---|---|---|---|
| `WIN_NORMAL` 등 canonical reason 토큰 | `§07 RESULT` | 공통 §4 | **G11** |
| 몰수 사유 `재접속 유예 종료` | `§07` · `05-04` | 공통 §4 | **G11** — `disconnected_timeout`은 **이미 기록된다** (§3.4.1). 표시 매핑만 없다 |
| 그룹 최종 순위의 완주/리타이어/몰수/기권 구분 | `§07` · `05-04` | 공통 §4 | **G11** |
| 5슬롯 상시 표시 + 쿨타임/소비/지속 상태 | `§04` · `04-03` | 14 | **서버 인벤토리 부재** — §1.1 |
| 주간 XP 순위 · 누적 XP · 레벨 진행 | `§02` · `02-02`·`02-03` | 15 | 전부 신규 |
| 3코스 진행 상태 | `§02` · `07-22` | 17a-1 | **G3** |
| 업적 자동 지급 표시 · NEW 배지 | `§07` · `05-06` | 16 | **G9** |

### 4.4 확정값과 일치함을 확인한 축

그룹 3~8인 · grace 2분 / 제한 20분 · 아이템 5슬롯 · 공통 쿨타임 2.5초 · 그룹 아이템 없음 ·
빠른 링크 최대 20 · 1:1 재접속 60초 · 1:1 XP(승 50 / 패 25 / 몰수승 30 / 기권 0) ·
주간 랭킹(업적 XP 제외 · 월요일 00:00) · 레벨 무한·능력치 무관 ·
프로필 꾸미기 범위(아이콘 1 · 칭호 1 · 배지 ≤3 · 프레임 1 · 배경 1) ·
금지 범위(상점·재화·업로드·4부위·텍스트 채팅).

> **stale 문서의 15분/3분이 시안에 없다.** 두 시안 모두 확정값 20분/2분을 쓴다.

---

## 5. 오늘 3코스 전환 — 판정

### 5.1 현재 스키마는 3코스를 담을 수 없다 `[코드]`

```
20260730170602_baseline_remote_schema.sql:749
  ADD CONSTRAINT "daily_challenges_challenge_date_key" UNIQUE ("challenge_date");
```

**`challenge_date`에 UNIQUE가 걸려 한 날짜에 2행 이상 들어갈 수 없다.**
RPC 수정으로 해결되지 않는다 — **제약을 교체하는 migration이 필수다.**

| 대상 | 3코스 수용 | 막는 것 |
|---|---|---|
| `daily_challenges` | **불가** | `UNIQUE(challenge_date)`. `course_slot` 컬럼도 없다 |
| `daily_challenge_pool` | **부분** | 행이 start·target **쌍으로 미리 묶여** 있다 (`sort_order` PK, `UNIQUE(target_title)`). 확정 §3.3의 **"시작 후보 × 목표 후보를 매일 무작위로 연결"** 을 표현할 수 없다. 게다가 `DAILY_POOL_INSERT.sql`이 **`start_title`을 전부 `null`로 넣는다** — 시작 후보 풀 자체가 없다 |

**최소 변경 3건:** ① `UNIQUE(challenge_date)` → `UNIQUE(challenge_date, course_slot)`,
② `course_slot` 컬럼 + `1..3` CHECK, ③ 시작 후보 풀 신설 또는 pool 구조 변경.

### 5.2 RPC와 클라이언트 `[코드]`

`ensure_today_daily_challenge`의 **반환 형태는 이미 `TABLE(...)`이고 그날 행 전부를 돌려준다**
(`baseline:107-114`). **시그니처는 막지 않는다.** 막는 것은:

| 지점 | 문제 |
|---|---|
| `baseline:83-88` | `order by random() limit 1` — **한 개만 고른다** |
| `baseline:101-105` | `where not exists (…그날 행이 있으면)` — **"그날 아무 행"** 가드라 제약을 풀어도 1행만 들어간다 |
| 반환 컬럼 | **`course_slot`이 없어 클라이언트가 코스를 구분할 수 없다** |
| `dailyChallengeService.js:81` | **`ensuredData[0]`만 취해 단일 객체로 접는다.** 3행이 와도 나머지는 조용히 버려진다 |
| `dailyChallengeService.js:93-97` | fallback이 `.maybeSingle()` — **3행이면 오류**가 나고 하드코딩 `FALLBACK_DAILY_POOL`로 떨어진다 (G14) |

**소비자는 `pages/MainPage.jsx:159` 한 곳뿐**이라 클라이언트 변경 범위는 좁다.

> **경합 주의.** 현재 insert 가드는 `on conflict`가 아니라 `where not exists`다.
> UNIQUE 제약이 지금은 이중 안전장치인데, **제약을 교체하면 그 보호가 사라진다.**
> 가드를 함께 다시 써야 동시 호출에서 중복 삽입이 나지 않는다.

### 5.3 판정 — **17의 일부이나, 떼어내 앞세운다**

**문서상으로는 17 §3의 TODO다.** 그리고 `CLAUDE_HANDOFF.md:162`와
`PROD-SNAPSHOT-2026-08-20.md:96`이 이미 "하루 1코스 — 확정 스펙의 3코스와 불일치"로 등재해 두었다.

**그런데 두 가지가 이것을 17 안에 두기 어렵게 만든다:**

1. **15가 여기에 종속된다.** 코스별 독립 랭킹과 코스별 최초 완주 XP가 `course_slot` 없이는
   정의되지 않는다. 17 전체를 기다리면 15가 막힌다.
2. **창이 필요하다.** 제약 교체는 운영 DDL이므로 **새 cutover 창 + 건별 승인**이다
   (`AGENTS.md` §1). 17의 프로필 카드(17b)와 배포 단위가 다르다.

→ **`17a-1 오늘 3코스`로 떼어낸다. 패킷 14~17 중 DB 창이 확정적으로 필요한 첫 항목이다.**

### 5.4 잠금 해제 순서 — **정의된 적이 없고, 필요하지도 않다**

저장소 전수 검색 결과 **0건**이었고, **2026-09-02에 시안 오류로 판정됐다** (G12).
**추가 테이블 없이 완주 기록 조회로 진행 표시를 만든다.**

### 5.5 창 시점과 규모 `[사용자 결정 + 추정, 2026-09-02]`

**시점: 15 착수 후, 코스별 랭킹에 도달했을 때.**
15의 XP ledger·레벨·주간 랭킹은 3코스와 무관하므로 **먼저 진행한다.**
막히는 것은 15 §5.1(코스별 독립 랭킹) 하나뿐이고, 그 지점에서 창을 연다.

#### 5.5.1 창 규모 `[추정]`

| 항목 | 예상 |
|---|---|
| migration 수 | **1~2개.** ① 제약 교체 + `course_slot` + 시작 후보 풀, ② `ensure_today_daily_challenge` 교체. 한 파일로 묶어도 된다 |
| DDL 성격 | `drop constraint` + `add constraint` + `add column` + `create or replace function`. **`drop table`·`drop function`은 없다** |
| 대상 행 수 | `daily_challenges`는 **하루 1행씩 쌓인 작은 테이블**이다. 락 시간은 무시할 수준 `[추정]` |
| 함께 묶을 것 | **창 범위가 4항목으로 확정됐다** `[사용자 확정, 2026-09-02]` — ① 이 항목 ② **C4 CHECK 1건**(`game_records.result_status`. `match_end_reason`은 C4 §4.2가 제외) ③ **C3-① `profiles` 컬럼 단위 grant 축소 3컬럼** ④ **15b `profiles.total_xp` 추가**. ~~**G2-②**(`room_events` INSERT 회수)~~ → **제외됐다** (§3.3-2). 적용 순서와 절차 초안은 `docs/agent/TRACKS.md` §7 |

#### 5.5.2 유지보수 게이트 없이 가능한가 — **가능해 보인다. 8월 창과 성격이 다르다**

| 축 | 2026-08-27~28 창 | **3코스 창** |
|---|---|---|
| 함수 삭제 | **있었다** — `finish_group_player`·`update_group_progress`. 구버전 클라이언트가 호출 중이면 즉시 깨진다 | ~~**없다.** `ensure_today_daily_challenge`는 **교체**되고 시그니처가 유지된다~~ → **⚠ 정정 (2026-09-02): `drop function`이 필요하다.** 반환 `table(...)`에 `course_slot`을 추가하면 **반환 타입이 바뀌어 `create or replace`가 거부된다** `[코드]`. **다만 같은 트랜잭션 안에서 재생성하고 이름·인자가 유지되므로 8월의 삭제와 성격이 다르다** — 진짜 위험은 **`drop`이 ACL을 지우는 것**이다 (`anon` EXECUTE = 게스트의 오늘 코스). `TRACKS.md` §7.2 |
| 게이트가 필요했던 이유 | **구버전 세션 drain** (U3). 게이트가 그 역할을 대신했다 | **drain할 대상이 없다** |
| 게임 진행 의존 | migration 11개가 방·이동·결과 경로 전부를 바꿨다 | **`daily_challenges`는 레이스 중에 읽히지 않는다.** MainPage 진입 시 1회뿐 (`MainPage.jsx:159`) |
| FK·참조 | 광범위 | **없다.** 창 이후 migration 11개는 `daily_challenges`를 **한 번도 참조하지 않고**, `game_records`·`single_game_runs`에 코스 참조 컬럼이 없다 `[코드]` |
| publication | 4테이블 변경 대상 | **`daily_challenges`는 발행되지 않는다** → realtime 영향 0 |
| RLS | 정책 다수 재작성 | **정책 1개**(`Anyone can read daily challenges`, SELECT) |
| 구버전 프론트 영향 | **깨졌다** | **깨지지 않는다.** `ensuredData[0]`이 3행 중 첫 행을 집으므로 **1코스만 보이는 상태로 정상 동작**한다 |

> **판정: 유지보수 게이트 없이 실행 가능한 것으로 읽힌다** `[추정]`.
> **다만 창 자체는 필요하다** — 운영 DDL이므로 `AGENTS.md` §1의 건별 승인 대상이고,
> `CUTOVER-PLAN.md`의 W2(백업)·W5(dry-run)·W6(적용)·W7(검증)은 그대로 밟는다.
> **생략 후보는 W0(게이트 on)·W1(프론트 선배포)·W10(게이트 해제)뿐이다.**
>
> **최종 판단은 창을 여는 세션이 한다.** 위 표는 근거이지 승인이 아니다.

#### 5.5.3 배포 순서가 8월과 반대다

**8월 창의 교훈은 "프론트 먼저"였다** (W1-b → W8-b). **3코스는 반대다.**

| 순서 | 결과 |
|---|---|
| **DB 먼저** | 구버전 프론트가 3행 중 `[0]`을 집어 **1코스만 표시**한다. 기능이 늦게 보일 뿐 깨지지 않는다 |
| 프론트 먼저 | 신버전 프론트가 `course_slot`을 기대하는데 DB가 1코스·컬럼 없음 → **표시가 깨지거나 빈 상태**가 된다 |

> **방향은 고정된 규칙이 아니라 "어느 쪽이 우아하게 낡는가"로 정해진다.**
> 8월에는 옛 Edge Function이 새 프론트의 플래그를 무시해 안전했고, 여기서는 옛 프론트가
> 새 DB의 여분 행·컬럼을 무시해 안전하다. **매번 이 질문을 다시 한다.**

> **경합 주의 (재확인).** 제약을 교체하면 `where not exists` 가드의 이중 안전장치가 사라진다.
> **가드를 `on conflict (challenge_date, course_slot)`로 함께 다시 쓴다.**

---

## 6. 착수 순서

### 6.1 계약 고정의 단위는 패킷이 아니다

**G1·G4·G6·G10·G11은 전부 "두 패킷이 같은 것을 각자 정의한다"는 형태다.**
패킷 단위로 고정하면 이 다섯은 어느 패킷에서도 닫히지 않는다.

**`10-CODE-MASTER-TODO.md` §4 공통 데이터·서비스 TODO가 이미 그 자리다** —
"XP 규칙 카탈로그 분리", "보상 카탈로그·bundle ID·inventory·장착 상태 분리",
"아이템 정의 카탈로그 분리", "`FINISHED`/`FORFEIT`/`RETIRE`/`CANCELLED` 의미 통일"이 전부 거기 있다.
**§4를 패킷 0으로 먼저 고정한다.**

### 6.2 웨이브

| 웨이브 | 트랙 | 내용 | 병렬 |
|---|---|---|---|
| ~~**W0**~~ | 단일 | ~~공통 계약~~ → **문서 동결 완료 (2026-09-02).** `docs/contracts/` C1~C5. **남은 것은 G7과 계약이 드러낸 4건(§3.2.1)이며, 전부 해당 트랙 착수 전에 답하면 된다 — 더 이상 직렬 병목이 아니다** | **해소** |
| **W1** | **A** | **14 — 아이템 서버 권위.** 전송은 `room_events`로 확정(**G2**) → **창 불필요.** 남은 판단은 event_type 명명·INSERT 권한·`mini_game_*` 처리 (§3.3) | **2~3트랙** |
| | **B** | **15 — XP ledger · 레벨 · 주간 랭킹 · 레벨 랭킹.** **3코스와 무관하므로 먼저 간다** | |
| | **C** | **17a-2 기록·게스트** (3코스를 제외한 부분) | |
| **W2** | 단일 | **17b 프로필 카드·장착** (G1이 공통으로 빠졌으므로 W1과 병렬 가능) | |
| **W2′** | **창** | **17a-1 오늘 3코스 — cutover 창.** **15가 코스별 랭킹에 도달했을 때 연다** (§5.5). ~~G11-②·G2-② DDL을 여기에 묶는다~~ → **4항목으로 확정: 17a-1 · C4 CHECK 1건 · C3-① grant 축소 · 15b `total_xp`.** **G2-②는 제외됐고 14 이후 별도 창이다** (§3.3-2) | 창 중 다른 트랙은 계속 진행 가능 |
| **W2″** | **창** | **G2-② `room_events` INSERT 회수 — 별도 창.** **14 완료 + 프론트 배포 + 클라이언트 경로 부재 확인이 선행 조건이다** (§3.3-2, `TRACKS.md` §7.4) | 단독. 규모는 정책 1개 drop + revoke |
| **W3** | 단일 | **16 — 업적·보상** | **불가.** 14·15·17 전부 소비 |

**동시 진행 가능: W1에서 2~3트랙.** 그 이상은 결과·프로필 화면에서 충돌한다.

> **W2′가 유일한 창이다.** G2가 `room_events`로 결정되면서 **14가 창에서 빠졌고**,
> G1이 공통 계약으로 빠지면서 **16·17b의 상호 대기가 없어졌다.**
> **세 결정이 창을 하나로 줄였다.**

### 6.2.1 W0 동결 후 열리는 트랙 (2026-09-02)

**계약이 문서로 동결됐으므로 아래가 지금 바로 열린다:**

| 트랙 | 착수 가능 | 선행 조건 |
|---|---|---|
| **C5 프로필 카드 구현** | **즉시** | **없다. DDL이 0이고 다른 계약을 기다리지 않는다** |
| **15 XP ledger·레벨** | **거의 즉시** | **C3-① 하나** (`profiles` update 경로) — 답이 나오면 C2·C3 DDL을 그대로 쓴다 |
| **14 아이템 서버 권위** | **거의 즉시** | **G7 하나** (아이템 ID). 창은 필요 없다 |
| **17a-2 기록·게스트** | **즉시** | 없다 |
| 16 업적·보상 | 아직 | 14·15·17 산출물을 소비한다. **다만 C1이 동결돼 테이블 설계 대기는 끝났다** |
| 17b 프로필 카드·장착 | **C5와 함께** | C1 RPC 구현 |

> **동시에 열리는 트랙이 2~3개에서 4개로 늘었다.** W0가 직렬 병목이었던 이유는
> "무엇을 만들지 몰라서"였는데 그것이 닫혔다. **남은 선행 조건은 트랙마다 하나씩이고
> 서로 독립이다.**

### 6.3 계약 고정 난이도 `[전부 추정]`

| 패킷 | 난이도 | 이유 |
|---|---|---|
| **15** | **가장 쉽다 — 여기부터** | 도메인 규칙이 완전 정의. 빠진 것은 타입·제약뿐 |
| **17a** | 쉬움 | §5.1이 현재 코드 대비 차이를 이미 적어 뒀다. **단 17a-1은 창이 필요하다** |
| **14** | 중간 | 규칙 완전 정의 + 서버 자산 존재. G2 하나가 변수 |
| **17b** | 중간 | G1 해소에 종속 |
| **16** | **가장 오래 걸린다** | 테이블 7 + 업적 31 + 이벤트 9. G5가 설계 난제 |

### 6.4 먼저 답이 필요한 것 — **이전 5건은 전부 답이 나왔다 (2026-09-02)**

| 이전 질문 | 결정 |
|---|---|
| ~~G1 소유권~~ | **공통 계약.** 어느 패킷에도 주지 않는다 |
| ~~G3 창 시점~~ | **15 착수 후, 코스별 랭킹 도달 시점** |
| ~~G11 통일 여부~~ | **통일하지 않는다. 5값 유지 + 표시 매핑** |
| ~~G2 전송 경로~~ | **`room_events`.** 창 불필요 |
| ~~시안 3건~~ | **G16 = 55 · G19 = `변칙` · G18 = 범위 밖** |

**지금 남은 질문:**

1. **G4** — XP ledger의 `source type` 값 집합. **16의 업적 XP를 gameplay XP와 가르는 축**이므로
   15 단독으로 정할 수 없다. W0에서 16의 요구를 함께 본다.
2. **G7** — 아이템 ID 최종 확정. **event_type 명명(§3.3)이 여기에 딸린다.**
3. ~~**G2-②** — `room_events`의 클라이언트 INSERT 권한을 회수할 것인가~~
   → **답이 나왔다 (2026-09-02). 회수하지만 순서가 뒤집혔다** `[사용자 확정]`:
   **14 → 프론트 배포 → 경로 부재 확인 → 별도 창(W2″)**. 3코스 창에서는 빠졌다 (§3.3-2).
4. **G6** — 레벨 컬럼을 `profiles`에 둘 것인가 별도 테이블에 둘 것인가.

---

## 7. 범위 밖 — 후속 검토 항목

**계약에 포함하지 않기로 결정된 것들이다. 삭제가 아니라 보류이며, 다시 열 때 여기서 시작한다.**

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| **G18** | **`오늘의 발견 / TODAY'S DISCOVERY`** — 모바일 HOME의 문서 추천 카드(이미지·제목·요약) | **범위 밖 (2026-09-02)** `[사용자 판정]`. **패킷 17 계약에 포함하지 않는다** | Freeze v1 `02-01 Home`에 없고 모바일 §02에만 있다. 확정 스펙 §3.3·`17` 어디에도 없다. **모바일 보드의 "새 기능 없음" 선언 위반** |

**다시 열 때 정해야 할 것:** 문서 선정 기준(수동 큐레이션 / 오늘 코스 연동 / 랜덤),
저장 위치(새 테이블 / `daily_challenges` 확장), 이미지 출처와 저작권,
게스트 노출 여부. **어느 것도 현재 정의돼 있지 않다.**

> **시안 파일에는 이 카드가 그대로 남아 있다.** 시안만 읽으면 구현 대상으로 오독되므로
> `docs/design/MOBILE-VALIDATION-CORRECTIONS.md` §3이 같은 판정을 시안 옆에 남겼다.

---

## 8. 참조

| 문서 | 역할 |
|---|---|
| `docs/agent/CURRENT.md` | **현재 상태의 단일 기준.** §5.0 C가 이 문서를 가리킨다 |
| **`docs/agent/TRACKS.md`** | **병렬 트랙의 단일 기준 (2026-09-02 신설).** 트랙 A~D 배타 화이트리스트 · 동결 파일 · **공유 자원 감사 7건** · **3코스 창 4항목과 절차 초안 T-1~T6** · 트랙별 티켓. **이 문서의 §5.5·§6.2를 실행 단위로 옮긴 것이다** |
| `docs/design/MOBILE-VALIDATION-CORRECTIONS.md` | 시안 정정 기록. **시안 오류 6건**(G12·G13·G15·G16·G19 + Freeze v1 내부 불일치 G17)의 판정 근거와 **범위 밖 1건**(G18). 미판정은 G20 하나 |
| `docs/design/*.html` | 시안 원본 2개. **수정하지 않는다** |
| `wiki-race-2.0-handoff/01-CONFIRMED-SPEC.md` | 게임 규칙 단일 기준선 |
| `wiki-race-2.0-handoff/code/10-CODE-MASTER-TODO.md` §4·§5 | **공통 계약(W0)의 원본 목록** |
| `wiki-race-2.0-handoff/code/14~17` | 패킷 계획 |
| `docs/ops/PROD-SNAPSHOT-2026-09-02.md` | 운영 상태. **§5.4가 publication 추가의 선행 조건을 정한다 (G2)** |
| `docs/ops/CUTOVER-PLAN.md` | 창 절차. **17a-1이 이것을 쓴다.** 개선점은 §0.-2 |
